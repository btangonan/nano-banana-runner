# NEVER commit real secrets. Use Secret Manager in prod.
PORT=8787
BIND_HOST=127.0.0.1
LOG_LEVEL=info
# If you must run locally without Secret Manager, set a dev key:
GEMINI_BATCH_API_KEY=__set_for_local_dev_only__